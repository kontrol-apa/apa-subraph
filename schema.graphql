type ApaInfo @entity {
  id: Bytes!
  owner: Bytes! # address
  rarity: BigInt!
  imageId: BigInt!
  tokenId : BigInt!
}

type CatchUpHelper @entity {
  id: Bytes!
  lastSyncedTokenId: Int!
  syncCompleted: Boolean
}
